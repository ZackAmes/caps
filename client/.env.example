# Cartridge RPC token (sk_...) authorizing RPC access from the browser.
VITE_CONTROLLER_RPC_KEY=

# NOTE: dev-mode account credentials are hardcoded in src/lib/dojo/client.ts
# (Sepolia test account). Remove/flip devMode=false before any real launch.

# Optional: defaults to the public URL in src/lib/dojo/torii.json.
# VITE_TORII_URL=http://127.0.0.1:8081
